const _sendBuffer = () => {
    if (bufferedData.length && wsRef.value && status.value === "OPEN") {
      for (const buffer of bufferedData)
        wsRef.value.send(buffer);
      bufferedData = [];
    }
  }
bufferedData.length && wsRef.value && status.value === "OPEN"
_iterator_12 = <operator>.iterator(bufferedData)
(_tmp_874 = wsRef.value).send
