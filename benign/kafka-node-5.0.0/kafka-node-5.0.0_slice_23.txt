const doSend = () => {
    this.queueCallback(broker.socket, correlationId, [decode, cb]);
    broker.write(request);
  }
this.queueCallback(broker.socket, correlationId, [decode, cb])
broker.write(request)
