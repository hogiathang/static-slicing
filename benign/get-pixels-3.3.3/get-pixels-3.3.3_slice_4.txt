function copyBuffer = function copyBuffer(buffer) {
  if(buffer[0] === undefined) {
    var n = buffer.length
    var result = new Uint8Array(n)
    for(var i=0; i<n; ++i) {
      result[i] = buffer.get(i)
    }
    return result
  } else {
    return new Uint8Array(buffer)
  }
}
buffer[0] === undefined
var n = buffer.length
var result = new Uint8Array(n)
var i=0
result[i] = buffer.get(i)
