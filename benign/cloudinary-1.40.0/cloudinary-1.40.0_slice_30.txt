fs.readdirSync(dir_path).forEach(function (entry) {
      let entry_path = path.join(dir_path, entry);
      if (fs.lstatSync(entry_path).isDirectory()) {
        rimraf(entry_path);
      } else {
        fs.unlinkSync(entry_path);
      }
    })
let entry_path = path.join(dir_path, entry)
(_tmp_1 = fs.lstatSync(entry_path)).isDirectory
fs.unlinkSync(entry_path)
