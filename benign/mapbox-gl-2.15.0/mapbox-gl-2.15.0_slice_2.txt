var fs = require("fs")
var minimist = require("minimist")
var format = require("@mapbox/mapbox-gl-style-spec").format
const argv = minimist(process.argv.slice(2))
argv.help || argv.h || (!argv._.length && process.stdin.isTTY)
help()
process.exit(0)
function help = function help() {
    console.log('usage:');
    console.log('  gl-style-format source.json > destination.json');
    console.log('');
    console.log('options:');
    console.log('  --space <num>');
    console.log('     Number of spaces in output (default "2")');
    console.log('     Pass "0" for minified output.');
}
