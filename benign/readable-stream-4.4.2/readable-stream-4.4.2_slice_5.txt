function resume_ = function resume_(stream, state) {
  debug('resume', state.reading)
  if (!state.reading) {
    stream.read(0)
  }
  state.resumeScheduled = false
  stream.emit('resume')
  flow(stream)
  if (state.flowing && !state.reading) stream.read(0)
}
debug('resume', state.reading)
!state.reading
stream.read(0)
state.resumeScheduled = false
stream.emit('resume')
