_closeFile(path) {
  const closers = this._closers.get(path);
  if (!closers) return;
  closers.forEach(closer => closer());
  this._closers.delete(path);
}
const closers = this._closers.get(path)
!closers
closers.forEach(closer => closer())
(_tmp_105 = this._closers).delete
