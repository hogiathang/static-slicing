var ms$3 = function (val, options) {
  options = options || {};
  var type = typeof val;
  if (type === 'string' && val.length > 0) {
    return parse(val);
  } else if (type === 'number' && isFinite(val)) {
    return options.long ? fmtLong(val) : fmtShort(val);
  }
  throw new Error(
    'val is not a non-empty string or a valid number. val=' +
      JSON.stringify(val)
  );
}
options = options || {}
var type = typeof val
type === 'string' && val.length > 0
type === 'number' && isFinite(val)
throw new Error(
    'val is not a non-empty string or a valid number. val=' +
      JSON.stringify(val)
  );
'val is not a non-empty string or a valid number. val=' +
      JSON.stringify(val)
