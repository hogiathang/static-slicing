wsEvent(packet) {
        switch(packet.t) { /* eslint-disable no-redeclare */ // (╯°□°）╯︵ ┻━┻
            case "PRESENCE_UPDATE": {
                if(packet.d.user.username !== undefined) {
                    let user = this.client.users.get(packet.d.user.id);
                    let oldUser = null;
                    if(user && (user.username !== packet.d.user.username || user.discriminator !== packet.d.user.discriminator || user.avatar !== packet.d.user.avatar)) {
                        oldUser = {
                            username: user.username,
                            discriminator: user.discriminator,
                            avatar: user.avatar
                        };
                    }
                    if(!user || oldUser) {
                        user = this.client.users.update(packet.d.user, this.client);
                        /**
                        * Fired when a user's avatar, discriminator or username changes
                        * @ev...
packet.t
"GUILD_MEMBER_UPDATE"
!(this.client.options.intents & Constants.Intents.guildPresences) && packet.d.user.username !== undefined
let user = this.client.users.get(packet.d.user.id)
let oldUser = null
user && (user.username !== packet.d.user.username || user.discriminator !== packet.d.user.discriminator || user.avatar !== packet.d.user.avatar)
oldUser = {
                            username: user.username,
                            discriminator: user.discriminator,
                            avatar: user.avatar
                        }
_tmp_164.username = user.username
_tmp_164.discriminator = user.discriminator
_tmp_164.avatar = user.avatar
!user || oldUser
user = this.client.users.update(packet.d.user, this.client)
this.emit("userUpdate", user, oldUser)
const guild = this.client.guilds.get(packet.d.guild_id)
!guild
let member = guild.members.get(packet.d.id = packet.d.user.id)
let oldMember = null
oldMember = {
                        avatar: member.avatar,
                        communicationDisabledUntil: member.communicationDisabledUntil,
                        roles: member.roles,
                        nick: member.nick,
                        premiumSince: member.premiumSince,
                        pending: member.pending
                    }
_tmp_168.avatar = member.avatar
_tmp_168.communicationDisabledUntil = member.communicationDisabledUntil
_tmp_168.roles = member.roles
_tmp_168.nick = member.nick
_tmp_168.premiumSince = member.premiumSince
_tmp_168.pending = member.pending
member = guild.members.update(packet.d, guild)
this.emit("guildMemberUpdate", guild, member, oldMember)
