sendWS(op, data) {
        if(this.ws && this.ws.readyState === WebSocket.OPEN) {
            data = JSON.stringify({op: op, d: data});
            this.ws.send(data);
            this.emit("debug", data);
        }
    }
this.ws && this.ws.readyState === WebSocket.OPEN
data = JSON.stringify({op: op, d: data})
(_tmp_61 = this.ws).send
this.emit("debug", data)
